% The protocol does not duplicate messages
% Obtained from: "Tools for parameterized Boolean Equation Systems",
% S. Janssen, MSc. thesis
%
% Note that this property as presented here does not hold for the onebit
% protocol. In general,
% it only holds for protocols that represent a one-place buffer, whereas the
% onebit protocol behaves as a two-place buffer.
[true*] forall d:D. [ra(d).(!ra(d) && !sb(d))*.sb(d).(!ra(d))*.sb(d)]false
